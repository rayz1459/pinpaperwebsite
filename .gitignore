# Runtime data (download counter)
download-stats.json

# Installer binaries — too big for git/Vercel; uploaded as GitHub Release assets instead
*.exe
*.dmg
*.dmg.sha256

# macOS
.DS_Store

# Logs / deps
node_modules/
*.log

# Local-only (kept on disk, not published)
brand_assets/
CLAUDE.md
